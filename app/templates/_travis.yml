language: node_js
sudo: false

node_js:
  - '0.10'

script:
  - npm run lint<% if (config.codeCoverage) { %>
  - npm run istanbul && cat ./coverage/lcov.info | coveralls<% } else { %>
  - npm test<% } %>
